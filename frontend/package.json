{
  "name": "multimedia-gallery-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --port 3001",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "lighthouse": "lhci collect && lhci assert",
    "test:all": "npm run audit && npm run cypress:run && npm run lighthouse"
  },
  "dependencies": {
    "@fontsource/plus-jakarta-sans": "^5.2.8",
    "axios": "1.6.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-router-dom": "6.14.0",
    "react-toastify": "^11.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "5.2.0",
    "cypress": "^14.0.0",
    "@lhci/cli": "^0.14.0"
  }
}
