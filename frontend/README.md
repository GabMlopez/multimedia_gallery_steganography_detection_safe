# Frontend (React + Vite)

Instrucciones rápidas:

```bash
cd frontend
npm install
npm run dev
```

- La app usa proxy `/api` -> `http://localhost:8080` (ver `vite.config.js`).
- Para producción puedes ejecutar `npm run build` y copiar la carpeta `dist` a `src/main/resources/static` del backend o servirla separadamente.
